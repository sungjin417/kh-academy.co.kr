패키지 = 폴더
클래스 = 파일
메소드 = 동작
함수와 메소드는 비슷(거의동일)
(함수가 클래스에 속하면 메소드)
자바에서는 함수 존재 X
컴파일러에서는 메인메소드가 존재해야함
int switch; // switch가 예약어라서 안됨
int 23rate; // 숫자가 앞에 오면 안됨
int jobs; // 변수이름은 소문자로 지어야 함(에러는 안나지만 관례임) 클래스이름은 대문자로 (설계도면)
int year = 2024; // 선언과 동시에 값을 할당
int day = 0; // 선언과 동시에 값을 0으로 초기화
String name = ""; // 이름이라는 문자열을 담는 변수를 선언하고 초기화(null=""의미는 다름)
(String은 참조형 (주소개념))_ 앞에 대문자면 참조형(ex. String)
        
int n = 20;
if(n > 10) {
int m = 30;
m = n - 10;
        }
int k = n + m; // m은 이미 메모리에서 사라지고 없음

리터럴 상수 : 문자열이 같으면 변수이름이 달라도 같은 주소를 가지고 있다.
실수형 : 지수와 가수로 표현되면 가수 부분이 정밀도 연관이 있음, 근사치 표기법

System.out.println("총점 : " + kor + eng + mat); // 더하기 연산자에서 문자열 다음에 오면 문자열로 인식해서 이어붙임
("총점 : "을 빼면 처음오는게 정수라서 제대로 된 더하기를 함. 즉 괄호를 치면 괄호부터 먼저 연산하기 때문에 정상적으로 출력 됨)