// 딕셔너리 = Hash Map
dictionary = Map =    (객체리터럴)	≠ map(고차함수)
파이썬       자바     자바스크립/리엑트     파이썬/리엑트
{    }       Hash,Tree    	       ＼       /
    ＼       /		      Json(+ http + Axio)
    키, 값(value)

//함수
- 재활용이 가능해야함(1번만 쓰면 안됨)
- 일반적으로 식별자뒤에 ()소괄호가 있으면 함수
- 함수는 내부에 값을 가질 수 없다/ 
  객체는 내부에 각각 값을 가질 수 있음
(자바는 디폴트 매개변수가 없다, 오버로딩이라서)

// 람다(수학공식)


