생명주기 : 화면에 나타나고 사라지는 일련의 과정
파일,컴퍼런트 이름은 대문자로 쓰는게 관례!!
useCallback은 콜백 함수를 메모이제이션하고, useMemo는 값 자체를 메모이제이션합니다. 
일반적으로 콜백 함수 최적화에는 useCallback을 사용하고,
계산 비용이 높은 값을 최적화하려면 useMemo를 사용합니다.

// 명령어
pwd : 패스 확인
yarn add sass : sass 실행
index.js에서 
root안에 지워준다(아니면 2번 실행)